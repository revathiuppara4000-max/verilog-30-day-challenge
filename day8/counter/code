code

module up_counter(clk,reset,q);
input clk,reset;
output reg [3:0] q;
always @(posedge clk or posedge reset)
begin
if(reset)
q<=4'b0000;
else
q<=q+1;
end
endmodule

//tb

module tb;
reg clk,reset;
wire [3:0] q;
up_counter u(clk,reset,q);
initial
begin
clk=0;
reset=1;
#10 reset=0;
end
always #5 clk=~clk;
initial
begin
$dumpfile("dump.vcd");
$dumpvars;
#100 $finish;
end
endmodule
