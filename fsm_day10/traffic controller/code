module traffic_controller(
    input clk,
    input reset,
    output reg [2:0] light
);
parameter RED = 2'b00;
parameter GREEN = 2'b01;
parameter YELLOW = 2'b10;
reg [1:0] state;
reg [3:0] count;
always @(posedge clk or posedge reset)
begin
    if(reset)
    begin
        state <= RED;
        count <= 0;
    end
    else
    begin
        case(state)
        RED:
        begin
            if(count == 5)
            begin
                state <= GREEN;
                count <= 0;
            end
            else
                count <= count + 1;
        end
        GREEN:
        begin
            if(count == 5)
            begin
                state <= YELLOW;
                count <= 0;
            end
            else
                count <= count + 1;
        end
        YELLOW:
        begin
            if(count == 2)
            begin
                state <= RED;
                count <= 0;
            end
            else
                count <= count + 1;
        end

        endcase
    end
end
always @(*)
begin
    case(state)
    RED: light = 3'b100;
    GREEN: light = 3'b001;
    YELLOW: light = 3'b010;
    default: light = 3'b000;

    endcase
end
endmodule


  //tb
  module traffic_controller_tb;
reg clk;
reg reset;
wire [2:0] light;
traffic_controller t1(clk, reset, light);
always #5 clk = ~clk;
initial
begin
    $dumpfile("dump.vcd");
    $dumpvars(0, traffic_controller_tb);
    clk = 0;
    reset = 1;
    #10 reset = 0;
    #100 $finish;
end
endmodule
